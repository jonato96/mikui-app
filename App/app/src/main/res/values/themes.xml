<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Mikui" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Mikui2" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="TemaReconocimiento" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#FF206C</item>
        <item name="colorPrimaryVariant">#FF5722</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Customize your theme here. -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">#FF5722</item>
    </style>

    <style name="ShapeAppearanceOverlay.CornersSize" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
        <item name="android:strokeWidth">5</item>
        <item name="android:strokeColor">@color/white</item>
    </style>

    <style name="SplashTheme2" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_back</item>
    </style>

    <style name="ShapeAppearanceOverlay.App.CornerBottom" parent="">
        <item name="cornerSizeBottomLeft">20%</item>
        <item name="cornerSizeBottomRight">20%</item>
    </style>

    <style name="ShapeAppearanceOverlay.App.CornerFull" parent="">
        <item name="cornerSize">50%</item>
    </style>

    <!-- Base Theme for transparent window -->
    <style name="Theme.Mikui2.Transparent">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Base Theme for ActivityDetail -->
    <style name="Theme.Mikui2.Transparent.ActivityDetail"/>
    <!-- Base Theme for ActivityDetail -->
    <style name="TemaReconocimiento.ResultActivity"/>


</resources>